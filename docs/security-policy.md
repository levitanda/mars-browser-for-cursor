# Security policy (draft)
Report vulnerabilities privately. Sensitive operations require explicit approval modes. Default-deny policies block destructive and cross-domain actions unless allowed by user policy.
