# Generic MCP client setup
- Claude Desktop: configure stdio command to `node .../native-host/dist/mcp-stdio.js`.
- VS Code MCP: same stdio command.
- Optional HTTP clients: explicitly enable localhost HTTP mode and provide token/origin.
