# Cursor MCP setup

```json
{
  "mcpServers": {
    "mars-browser": {
      "command": "node",
      "args": ["/absolute/path/native-host/dist/mcp-stdio.js"]
    }
  }
}
```

Example prompts:
- open localhost:3000 and verify the signup flow
- run manual QA on the checkout modal
- inspect console errors after clicking Save
- extract all pricing cards into JSON
