# Troubleshooting
- Host not detected: verify manifest location and extension id in `allowed_origins`.
- MCP client cannot connect: confirm stdio command points to `mars-mcp-server` binary.
- No tab actions: ensure selected tab was set and page is not restricted (`chrome://` etc).
