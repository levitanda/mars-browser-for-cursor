# Release checklist
- Run `pnpm build && pnpm test && pnpm verify:publishability`
- Validate unpacked extension install
- Validate native host install/uninstall on macOS/Linux/Windows
- Update changelog and tag release
